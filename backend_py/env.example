## Python backend env (DO NOT commit real secrets)

# Server
APP_HOST=0.0.0.0
APP_PORT=3001

# PostgreSQL (SQLAlchemy URL)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/admarketplace

# Telegram bot token (secret) — needed to validate initData signature
TG_BOT_TOKEN=PUT_YOUR_TOKEN_HERE

# initData freshness window (sec)
TG_AUTH_MAX_AGE_SEC=86400

# JWT (for frontend session)
JWT_SECRET=change_me
JWT_EXPIRES_SEC=2592000

# Internal secret for local bot -> backend (phone contact saving)
INTERNAL_SECRET=dev_internal_secret_change_me

# API base URL for bot to call backend (channel-added etc). Default: http://127.0.0.1:3001
API_BASE_URL=http://127.0.0.1:3001

# Mini App URL for bot's "Open App" button. Use full HTTPS URL (e.g. https://teamwb.top) or leave default (t.me/botname/app)
# WEBAPP_URL=https://teamwb.top

# Order verification (24h/48h): channel ID where bot forwards posts to check they exist. Bot must be admin.
AD_VERIFICATION_CHANNEL_ID=-1003801222498

# TON native deposits: our wallet receives TON. Users send from their CONNECTED wallet.
# TON_DEPOSIT_WALLET=UQ...
# USDT deposits (TON network). Get your USDT Jetton wallet address from Tonkeeper.
# USDT_DEPOSIT_WALLET=EQ...
# TonAPI key for scanning incoming transfers (https://tonapi.io)
# TONAPI_KEY=

# USDT withdrawals: hot wallet for sending. Private key (hex) ИЛИ mnemonic (24 words).
# Приоритет у private key. Получить: MyTonWallet → Settings → Security → View TON Private Key
# USDT_WITHDRAW_PRIVATE_KEY=hex64_or_hex128
# USDT_WITHDRAW_MNEMONIC=word1 word2 ... word24
# Hot wallet address (for verification, обязателен при private key)
# USDT_WITHDRAW_WALLET=UQ...


